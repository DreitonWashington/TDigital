import styled from "styled-components";

export const Footer = styled.footer`
    background: rgba(26, 26, 26, 1);
    width: 100%;
    height: 7.5rem;
    margin-top: 10.7rem;
    position: relative;

    p{
        color: white;
        font-size: 0.7rem;
        text-align: end;
        position: absolute;
        right: 8rem;
        top: 3.5rem;
    }
`